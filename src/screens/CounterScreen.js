import React from 'react'
import { Text, FlatList, StyleSheet, View, Counter } from 'react-native'

const CounterScreen = () => {
    return <View>
        <Text>Current Count</Text>
    </View>
}
const styles = StyleSheet.create({

})
export default CounterScreen